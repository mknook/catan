package model;

public class City extends Village {
	// this is a dummy class to be replaced by the real city class
	// the information used to draw is stored here, NOTHING ELSE
	// to keep the board graphics functional use at least these variables and
	// methods:
	public City(char colour) {
		super(colour);
	}
}
